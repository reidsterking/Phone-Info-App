{
  "name": "device-tracker-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend and web dashboard for the DeviceTracker iOS app",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  }
}
